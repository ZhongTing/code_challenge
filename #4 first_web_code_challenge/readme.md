以下 6 題，請任選 3 題回答(程式題目限使用python 或 java 語言實作)

1. 實作一個計算 Fibonacci number (https://en.wikipedia.org/wiki/Fibonacci_number) 的 Function
例如： fib(6) 的結果為 8
請用 Recursive 及 非 Recursive 的方式各作一次。

2. 給定一個 array, 例如：
10, 20, 50, 7, 9 
寫一個函數計算該 array 的中位數。

3. 寫一個函數, 計算2個矩陣相乘. 請自行使用迴圈計算, 禁止使用任何的函數庫計算.

4. 寫一個簡單的網路爬蟲程式, 到google 搜尋 nlp, parsing出每一筆結果的標題及以及url.

5. 請解釋什麼是Inverted index, 並描述在什麼情況下使用, 以下三個文件的inverted index會長什麼樣子. 
doc 1 : A goal of a search engine implementation is to optimize the speed of the query
doc 2 : Apache Lucene is a full-featured text search engine library written in Java 
doc 3 : sorting and searching algorithms have been characterized by the number of comparison operations

6. 在 Web application 的環境下，請問 Cookie 功用是什麼？請列舉一個例子說明之。